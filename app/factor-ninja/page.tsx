import FactorNinjaGame from "../components/FactorNinja/Game";

export const metadata = {
    title: "Factor Ninja - Math Masters",
    description: "Slice the composites, avoid the primes!",
};

export default function FactorNinjaPage() {
    return <FactorNinjaGame />;
}
